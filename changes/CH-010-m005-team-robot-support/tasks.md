- [ ] Inspect a real `teamrumble` collection jar (structure of its `.team` descriptor and member classes/properties) and the Tank Royale booter's team-launch mechanism (vendored booter sources in the local Maven cache) to confirm how `TEAM_ID`/`TEAM_NAME`/`TEAM_VERSION` reach a launched bot process — informs the next two tasks, no separate output.
- [ ] Extend `robots-wrapper/src/main/java/Main.java` to detect a `.team` descriptor alongside a jar's `.properties` entries, parse `team.members` (comma-separated FQCNs, duplicates allowed), and disambiguate repeated members the way classic does (name suffixing) — serves `TEAM-001`.
- [ ] For each team member, produce a bot directory carrying the shared team identity (`TEAM_ID`/`TEAM_NAME`/`TEAM_VERSION`, wired however the prior task's inspection found the booter expects) and, when the member class implements `robocode.Droid`, whatever bot-info marker Tank Royale needs to treat it as a droid — serves `TEAM-001`, `TEAM-003`.
- [ ] Add the per-battle name-to-id table to `robocode-api`'s `BotPeer`, built from Tank Royale's participant/handshake data, and route `ITeamRobotPeer.getTeammates()` / `isTeammate(String)` / `sendMessage(String, ...)` through it instead of the numeric-string-parsing placeholder — serves `TEAM-002`.
- [ ] Update `TeamAndJuniorPeerRoutingTest` (`ROUTE-009`) to exercise real names through the new table rather than raw numeric strings; `ROUTE-009`'s criterion text is unchanged (translated identity, refusal of an unaddressable name), only the routing mechanism it tests changes.
- [ ] Author purpose-written conformance test robots for `TEAM-002` (message delivery: broadcast reaches every member, a directed message reaches only its addressee) and `TEAM-003` (a droid receives no scan events on either engine while acting correctly on teammate-reported information) under the conformance tier — classic's own test suite has no team robots to port.
- [ ] Un-skip the team division in `compat-test/compat_test.py` (`tr_skipped = (collection == "teamrumble")` and its surrounding logic) now that the wrapper produces runnable team bot directories.
- [ ] Remove `@draft` from `TEAM-001`, `TEAM-002`, `TEAM-003` in `docs/capabilities/CAP-006-team-robot-support/criteria.md`.
- [ ] Update `docs/capabilities/CAP-006-team-robot-support/design.md` and `README.md` to describe the implemented shape (native Tank Royale team mapping, the name-to-id table) rather than the open decision; update `docs/architecture/README.md`'s `CAP-006` row and `ARCH-001`'s note that the one-jar-to-one-bot-directory assumption "has to give."
- [ ] Record `ADR-002` (map onto Tank Royale's native team model, per `AN-013`), linking `CAP-006`, `AN-013`, and the affected architecture overview.
- [ ] Update `P-001`'s milestone table (`M-005` to `done`) and evidence-door rows for `TEAM-001`, `TEAM-002`, `TEAM-003` (`M-129`–`M-131`).
- [ ] Run the tier-1 unit suite and the focused tier-2 conformance tests with the matched local Tank Royale API/runner pair; run the un-skipped team division through the harness; retain any environment limitation in the handoff.
- [ ] Digest: regenerate indexes, run `clue validate`, commit the final state, and delete this change workspace.
